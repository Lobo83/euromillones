
spring:
  datasource:
    url: jdbc:h2:mem:euromillones
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

    hibernate:
      ddl-auto: create
    #properties:
      #hibernate:
        #jdbc:
         # batch_size: 10
        #order_inserts: true
        #generate_statistics: true

